services:
  - type: web
    name: resource-hubz-forum
    runtime: docker
    plan: starter
    autoDeployTrigger: commit
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    disk:
      name: resource-hubz-data
      mountPath: /app/data
      sizeGB: 1
